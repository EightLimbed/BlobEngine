//breaks apart vectors and creates a list of their indices for each cardinal direction, before sorting them by that direction.
#version 430 core

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in; //64 local threads is apparently sweet spot

layout(std430, binding = 0) buffer ChunkData {
    uint numPoints;
    vec4 points[];
    uint xsort[];
    uint ysort[];
    uint zsort[];
};